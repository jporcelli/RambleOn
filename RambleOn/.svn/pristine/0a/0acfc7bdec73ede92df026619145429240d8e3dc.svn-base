package rambleon;

/**
 * Enumerated type to build on top of the underlying frame works basic
 * enumerated types already provided. This set of enumerations is used to
 * identify the specific modes of game play as opposed to the more general game
 * started, game not started , win , loss, types provided by the frame work.
 *
 * @author James Porcelli, SBU ID 108900819
 */
public enum RambleOnModeType {

    CAPITAL_MODE, LEADER_MODE, NAME_MODE, FLAG_MODE, REGION_SELECTION
}
